from setuptools import setup, find_packages

setup(
    name='lookalike_fuzzer',
    version='0.1.0',
    description='technik nai karn thod-sob software',
    packages=find_packages(),
    install_requires=[
        'idna',
    ],
)
